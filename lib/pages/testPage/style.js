import { Colors } from '../../config/constants';
export default {
    container: {
        flex: 1,
        alignItems: 'center',
        flexDirection: 'column',
        backgroundColor: Colors.white
    }
};
